Model
→ 连接大模型

Messages
→ 承载上下文

System Prompt
→ 软约束行为

Tool schema
→ 控制工具输入

Agent loop
→ 自动选择并循环调用工具

Multi-tool
→ 完成多步骤任务

Checkpointer + thread_id
→ 控制会话记忆

Runtime Context
→ 安全传递用户身份和权限

Human-in-the-loop
→ 控制危险操作

Streaming
→ 观察实时执行过程

Response format
→ 控制最终输出

StateGraph
→ 控制整个业务流程

Persistent Checkpointer
→ 持久化会话和任务状态

Retry + Guardrails
→ 提高稳定性和安全性

RAG
→ 接入私有知识库

LangSmith + Evals
→ 调试、监控和测试Agent

Express + SSE
→ 提供流式后端接口

Vue
→ 实现聊天、进度和审批页面

MCP
→ 标准化接入外部工具

Multi-agent
→ 最后再学习多Agent协作


StateGraph + 局部 Tool 循环
→ Reducer
→ Command
→ Subgraph
→ 持久化 Checkpointer
→ Retry / Timeout / 幂等
→ LangSmith / Evals
→ Express + SSE + Vue