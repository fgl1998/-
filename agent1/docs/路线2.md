| 顺序 | 学习内容          | 学完能解决什么                       |
| -- | ------------- | ----------------------------- |
| 1  | Tool 异常处理     | 工具报错后，不让整个工作流直接崩溃             |
| 2  | 官方 `ToolNode` | 用框架提供的节点替换手写工具分发代码            |
| 3  | 循环次数限制        | 防止 AI 一直调用工具造成死循环             |
| 4  | SystemMessage | 规定 AI 什么时候调用工具、什么时候直接回答       |
| 5  | 信息不足时追问       | 参数不完整时先询问用户，而不是胡乱调用 Tool      |
| 6  | 自定义 State     | 除了 `messages`，还能保存用户、权限、当前操作等 |


| 顺序 | 学习内容                | 学完能解决什么                     |
| -- | ------------------- | --------------------------- |
| 7  | 固定流程与 Agent 循环结合    | 只有部分节点允许 AI 自主选择            |
| 8  | 查询 Tool 与写入 Tool 分离 | 查询可以直接执行，删除、修改必须严格控制        |
| 9  | 权限校验                | 防止用户修改、删除别人的文章              |
| 10 | 敏感操作确认              | 删除、发布等操作使用 `interrupt` 二次确认 |
| 11 | Tool 调用 Service 层   | Tool 不直接操作数据库，复用原有业务逻辑      |
| 12 | 幂等处理                | 防止重复创建、重复删除、重复提交            |
| 13 | 超时、重试和降级            | 接口或模型失败时进行合理处理              |


| 顺序 | 学习内容              | 学完能解决什么             |
| -- | ----------------- | ------------------- |
| 14 | `thread_id`       | 区分不同用户和不同会话         |
| 15 | Checkpointer 持久化  | 服务重启后仍然保留会话状态       |
| 16 | 中断后的恢复            | 用户确认后从原位置继续运行       |
| 17 | `getState`        | 查看当前会话状态            |
| 18 | `getStateHistory` | 查看工作流每一步的历史状态       |
| 19 | Streaming         | 前端实时显示 AI 输出和工具执行过程 |
| 20 | Agent 日志与 Trace   | 定位 AI 为什么调用某个工具     |
